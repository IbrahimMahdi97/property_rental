<div class="container">
  <div class="row">
    <div class="col-sm-12">

      <h1 class="view-title">Editing Room <%= @room.title %>
      </h1>
      <%= link_to 'Show', @room, class:"show-tag" %><%= link_to 'Back', rooms_path, class:"back-tag" %>

      <%= render 'form', room: @room %>
    </div>
  </div>
</div>